# TernaryTreeDirectory
User-configurable file system traversal using a Scanner-based program, incorporating a cursor-linked interface to display the current command line prompt location (e.g., Home/User/downloads). 
